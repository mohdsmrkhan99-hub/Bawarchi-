import { getSettingsDashboard } from "./actions";
export const dynamic = "force-dynamic";
export default async function SettingsPage() {
  const data = await getSettingsDashboard();
  const links = [["/tables", "Tables & floors", `${data.modules.tables} active tables`], ["/menu", "Menu", `${data.modules.menuItems} active items`], ["/staff", "Staff", `${data.modules.staff} active staff`], ["/settings/printers", "Printers", `${data.modules.printers} configured printers`], ["/orders", "Orders", `${data.modules.openOrders} open orders`], ["/reports", "Reports", "Sales and payments"]];
  return <main className="min-h-screen bg-slate-50 p-4 text-slate-900 sm:p-8"><div className="mx-auto max-w-6xl"><h1 className="text-3xl font-black">Settings</h1><p className="mb-6 text-sm text-slate-500">Operational configuration and module shortcuts</p><section className="rounded-2xl border bg-white p-6"><h2 className="text-xl font-bold">{data.restaurant.name}</h2><div className="mt-3 grid gap-3 text-sm sm:grid-cols-2"><p><span className="text-slate-500">Restaurant code</span><br /><b>{data.restaurant.code}</b></p><p><span className="text-slate-500">Branch</span><br /><b>{data.branch.name} ({data.branch.code})</b></p><p><span className="text-slate-500">Phone</span><br /><b>{data.branch.phone || data.restaurant.phone || "Not configured"}</b></p><p><span className="text-slate-500">Address</span><br /><b>{data.branch.address || "Not configured"}</b></p></div></section><div className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{links.map(([href, label, detail]) => <a className="rounded-2xl border bg-white p-5 shadow-sm transition hover:border-slate-400" href={href} key={href}><h2 className="font-bold">{label}</h2><p className="mt-2 text-sm text-slate-500">{detail}</p><span className="mt-4 inline-block text-sm font-semibold">Open module →</span></a>)}</div></div></main>;
}
